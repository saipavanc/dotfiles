########################## hyprtasking ################################
# bind = Super, grave, hyprtasking:toggle, cursor
# bind = Super Shift, grave, hyprtasking:toggle, all

# bind = $mainMod, X, hyprtasking:killhovered
#
# bind = $mainMod, H, hyprtasking:move, left
# bind = $mainMod, J, hyprtasking:move, down
# bind = $mainMod, K, hyprtasking:move, up
# bind = $mainMod, L, hyprtasking:move, right
#
# plugin {
#     hyprtasking {
#         layout = grid
#
#         gap_size = 1
#         # bg_color = 255
#         border_size = 4
#         exit_behavior = active hovered interacted original
#     }
# }

################################### Pyprland ####################################
# bind = Super SHIFT, Z, exec, pypr zoom
# bind = $mainMod ALT, P,exec, pypr toggle_dpms
# bind = $mainMod SHIFT, O, exec, pypr shift_monitors +1
# bind = $mainMod, B, exec, pypr expose
# bind = $mainMod, K, exec, pypr change_workspace +1
# bind = $mainMod, J, exec, pypr change_workspace -1
# bind = $mainMod,L,exec, pypr toggle_dpms
# bind = Super SHIFT,M,exec,pypr toggle stb stb-logs
# bind = Super,A,exec,pypr toggle term

# bind = Super, M, exec, pypr layout-center toggle # toggle the layout
# # focus change keys
# bind = Super, left, exec, pypr layout-center prev
# bind = Super, right, exec, pypr layout-center next
# bind = Super, up, exec, pypr layout-center prev2
# bind = Super, down, exec, pypr layout-center next2

#bind = Super, M, exec, pypr monocle toggle # toggle the layout
# focus change keys
#bind = Super, left, exec, pypr monocle prev
#bind = Super, right, exec, pypr monocle next
#bind = Super, up, exec, pypr monocle prev2
#bind = Super, down, exec, pypr monocle next2

################### split workspace ##################
# plugin {
#     hyprsplit {
#         num_workspaces = 10
#     }
# }

####################################### Hycov Configuration #######################################

# bind key to toggle overview (normal), and also bring focussed floating window to top
# bind = ALT,tab,hycov:toggleoverview
# bind key to toggle overview (force mode, not affected by `only_active_workspace` or `only_active_monitor`)
# bind = ALT,grave,hycov:toggleoverview,forceall #grave key is the '~' key

# # bind key to toggle overview (shows all windows in one monitor, not affected by `only_active_workspace` or `only_active_monitor`)
# bind = ALT,g,hycov:toggleoverview,forceallinone 

# # The key binding for directional switch mode.
# # Calculate the window closest to the direction to switch focus.
# # This keybind is applicable not only to the overview, but also to the general layout.
# bind=ALT,left,hycov:movefocus,l
# bind=ALT,right,hycov:movefocus,r
# bind=ALT,up,hycov:movefocus,u
# bind=ALT,down,hycov:movefocus,d

# # if you want that focusmove can cross monitor, use this
# bind=ALT,left,hycov:movefocus,leftcross
# bind=ALT,right,hycov:movefocus,rightcross
# bind=ALT,up,hycov:movefocus,upcross
# bind=ALT,down,hycov:movefocus,downcross


# plugin {
#     hycov {
#         overview_gappo = 60 # gaps width from screen edge
#         overview_gappi = 24 # gaps width from clients
#         # enable_hotarea = 0 # enable mouse cursor hotarea, when cursor enter hotarea, it will toggle overview    
#         enable_click_action = 1 # enable mouse left button jump and right button kill in overview mode
#         # hotarea_monitor = all # monitor name which hotarea is in, default is all
#         # hotarea_pos = 1 # position of hotarea (1: bottom left, 2: bottom right, 3: top left, 4: top right)
#         # hotarea_size = 10 # hotarea size, 10x10
#         swipe_fingers = 4 # finger number of gesture,move any directory
#         move_focus_distance = 100 # distance for movefocus,only can use 3 finger to move 
#         enable_gesture = 1 # enable gesture
#         auto_exit = 1 # enable auto exit when no client in overview
#         auto_fullscreen = 0 # auto make active window maximize after exit overview
#         only_active_workspace = 1 # only overview the active workspace
#         only_active_monitor = 1 # only overview the active monitor
#         enable_alt_release_exit = 1 # alt swith mode arg,see readme for detail
#         alt_replace_key = Alt_L # alt swith mode arg,see readme for detail
#         alt_toggle_auto_next = 1 # auto focus next window when toggle overview in alt swith mode
#         click_in_cursor = 1 # when click to jump,the target windwo is find by cursor, not the current foucus window.
#         hight_of_titlebar = 0 # height deviation of title bar height
#         show_special = 0 # show windwos in special workspace in overview.
#     }
# }

################################ Hyprexpo ###########################################

# Overview
# plugin {
#     hyprexpo {
#         columns = 3
#         gap_size = 5
#         bg_col = rgb(000000)
#         workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1

#         enable_gesture = false # laptop touchpad, 4 fingers
#         gesture_distance = 300 # how far is the "max"
#         gesture_positive = true
#     }
# }


############################### Hyprspace ########################

# plugin {
#     overview {
#         panelHeight = 100
#         drawActiveWorkspace = true
#         hideOverlayLayers = true
#         affectStrut = true
#         exitOnSwitch = true
#         exitOnClick = true
#         # overrideAnimSpeed = 40
#     }
# }
